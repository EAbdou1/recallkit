import { FC } from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard - RecallKit",
  description: "Dashboard - RecallKit",
};

const Dashboard: FC = () => {
  return <div>Dashboard</div>;
};

export default Dashboard;
